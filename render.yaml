services:
  # 1. FastAPI Backend Service (Python Web Service)
  - type: web
    name: cookie-tracker-backend
    env: python
    plan: free
    buildCommand: |
      cd cookie/cookie-tracker-graph
      pip install -r requirements.txt
    startCommand: |
      cd cookie/cookie-tracker-graph
      uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0

  # 2. React Frontend Service (Static Site)
  - type: web
    name: cookie-tracker-frontend
    env: static
    buildCommand: |
      cd cookie/frontend
      npm install
      npm run build
    staticPublishPath: cookie/frontend/out
